export function leapYear(a) {

    let ly=a%4;

    if (ly==0) {
     console.log ("then it is leap year")
        
    } else {
      console.log( "it is not leap year")
    }
    return ly ;
    
}

